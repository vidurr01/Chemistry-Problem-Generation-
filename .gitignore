# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store

# Verifier Tier-1 dedup state (runtime)
verifier_seen_inputs.json

# runtime logs
*.log

# per-attempt generation trace (runtime, written next to each output file)
*_attempts.jsonl

# generation stop sentinel (runtime)
STOP_GEN
